# 打飞机游戏 — 动画帧循环起停路径与双重循环问题分析报告

> 分析对象：[script.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js)
> 现象：游戏偶尔出现“飞机突然加速、画面卡顿”，重启后恢复正常。
> 结论：**存在一条路径会导致两条 `requestAnimationFrame` 循环链同时存活，同一帧内更新两次，表现为加速；两条链调度错位会造成卡顿。**

---

## 一、循环机制基础

游戏动画由 [gameLoop()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L425-L445) 驱动，结构如下：

```js
function gameLoop() {
    if (!gameState.isPlaying || gameState.isPaused) {
        return;                       // ① 直接返回，不调度下一帧 → 循环“死亡”
    }
    updatePlayer();
    updateBullets();
    updateEnemies();
    spawnEnemy();
    checkCollisions();
    drawBackground();
    drawPlayer();
    drawBullets();
    drawEnemies();
    requestAnimationFrame(gameLoop);  // ② 只有走到这里，循环才“续命”
}
```

关键点：

1. **没有保存 rAF 返回的 ID，也没有任何 `cancelAnimationFrame` 调用。**
   - 全局搜索 `requestAnimationFrame|cancelAnimationFrame` 仅命中 4 处：[第75行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L75)、[第83行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L83)、[第425行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L425)、[第444行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L444)。
2. **循环的“生死”完全依赖状态标志**，而不是显式取消：
   - 当某一帧执行到 ① 处提前 `return`，它就不再调度下一帧，这条链就此终止。
   - 当某一帧走到 ② 处 `requestAnimationFrame(gameLoop)`，这条链延续到下一帧。
3. **运动是“每帧固定增量”**（如 `player.speed=5`、`bullet.speed=8`、`enemy.speed=2`），没有基于时间差（delta time）做归一化。一旦一帧内更新被执行多次，速度就会被成倍放大。

> 这套“靠状态标志让循环自然死亡”的设计，在单条链时是成立的；但它在“暂停→继续”快速切换时存在竞态窗口，会让旧链复活、与新链并存。

---

## 二、各路径循环起停时间线

### 路径 1：开始游戏 — [startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L70-L76)

- 置 `isPlaying=true`、`isPaused=false`。
- 禁用“开始”按钮、启用“暂停”按钮。
- 直接同步调用 `gameLoop()`。
- 该帧通过检查，走到 ② 调度下一帧 → **循环链 A 建立并持续运行**。
- 此时不存在其它链，**无问题**。

### 路径 2：暂停 — [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L78-L86) 的暂停分支

- `isPaused` 由 `false` 翻为 `true`，按钮文字改为“继续游戏”。
- **注意：函数本身并不调用 `gameLoop()`，也不取消已调度的帧。**
- 真正的停止发生在“下一次 rAF 回调触发时”：
  - 上一帧已经通过 ② 调度了一个待执行的回调（记为“在途帧”）。
  - 该在途帧触发 → 进入 `gameLoop` → 检查到 `isPaused=true` → 在 ① 处 `return`，**不再调度下一帧**。
  - 链 A 在这一帧后终止。
- 也就是说：点击暂停后，循环并不是“立刻”停，而是**多跑一帧后才停**。这一帧延迟正是后面竞态的根源。
- 在用户正常停顿（超过一帧间隔）后再继续，**无问题**。

### 路径 3：继续（解除暂停） — [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L78-L86) 的继续分支

- `isPaused` 由 `true` 翻回 `false`，按钮文字改回“暂停游戏”。
- 直接同步调用 `gameLoop()` → 建立一条新链（链 B）并持续运行。
- **关键问题**：这里默认“旧链已经死亡”，但旧链是否真的死亡，取决于“暂停时那个在途帧是否已经触发过”。
  - 若在途帧**已经触发**（暂停后至少经过了一个动画帧间隔）：它当时看到 `isPaused=true` 已自然终止 → 现在只有链 B，**正常**。
  - 若在途帧**尚未触发**（暂停与继续发生在同一个动画帧间隔内）：见第三节，**会产生双重循环**。

### 路径 4：游戏结束 — [checkGameOver()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L213-L221)

- 当 `lives<=0` 时置 `isPlaying=false`，显示结算层，禁用“开始/暂停”按钮（“重新开始”按钮仍可用）。
- 不在本函数内停循环，同样依赖在途帧：下一帧触发时检查到 `!isPlaying` → 在 ① 处 `return` → 链终止。
- 该路径**本身不会产生双重循环**，因为后续只能走“重新开始”。

### 路径 5：重新开始 — [restartGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L88-L114)

- 置 `isPlaying=false`、`isPaused=false`，清空子弹/敌机、重置玩家位置与分数、隐藏结算层、启用“开始”按钮、禁用“暂停”按钮、清空画布。
- **不调用 `gameLoop()`**。
- 旧循环的在途帧触发后因 `!isPlaying` 而终止。
- 用户随后需点击“开始”才会重建一条循环。
- 因此“重新开始”会**彻底清掉所有旧链**，这正是“重启后恢复正常”的原因（见第五节）。
- 该路径单独使用**无双重循环**；但与“开始”极快连点时存在次要风险（见第四节）。

---

## 三、双重循环触发路径（核心问题）

### 触发路径

**暂停 → 继续（快速连点 / 在同一动画帧间隔内完成）**，即对“暂停/继续”按钮进行一次快速双击：第一次点击暂停，第二次点击立即继续。

### 触发条件

- 在 [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L78-L86) 中，暂停动作仅置 `isPaused=true`，**不取消已通过 [第444行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L444) 调度的那个“在途帧”**。
- 当“继续”发生在该在途帧被浏览器派发**之前**（通常约 16ms 内；浏览器繁忙、掉帧时窗口会拉长到 33ms 甚至更长），旧链尚未走完“看到 `isPaused=true` 并自然死亡”的那一帧。
- “继续”把 `isPaused` 又改回 `false`，并直接同步调用 `gameLoop()` 启动新链 B。
- 随后那个**被延迟派发的旧在途帧终于触发**，它此时读到的是 `isPaused=false`、`isPlaying=true`，于是**不会在 ① 处返回，而是继续执行更新+绘制，并在 ② 处再次 `requestAnimationFrame` 自我续命** —— 旧链 A 复活。
- 最终：**链 A 与链 B 同时存活，各自独立地每帧调度下一轮**。

### 时序示意

```
时刻 t0 : 链 A 跑完一帧，调度“在途帧 A'”（约 t0+16ms 触发）
时刻 t1 : 用户点击“暂停” → isPaused=true （A' 尚未触发）
时刻 t2 : 用户快速点击“继续”（t2 < t0+16ms）
          → isPaused=false，同步调用 gameLoop() 启动链 B，调度 B'
时刻 t0+16ms : A' 触发，读到 isPaused=false → 不返回 → 更新+绘制+调度 A''
          （此时 A 链复活，与 B 链并存）
此后    : A 链、B 链各自 requestAnimationFrame，每帧各更新一次
```

### 为什么会“加速”

- 两条链在同一帧内都会执行一遍完整的更新逻辑：[updatePlayer()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L140-L153)、[updateBullets()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L155-L164)、[updateEnemies()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L166-L178)、[checkCollisions()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L180-L204) 各被调用一次。
- 由于移动是“每帧固定像素增量”而非基于 delta time，一帧更新两次 = 位移翻倍：
  - 玩家飞机速度约 2 倍（`playerSpeed=5` ×2）。
  - 子弹速度约 2 倍（`bulletSpeed=8` ×2）。
  - 敌机速度约 2 倍（`enemySpeed=2` ×2）。
- 这与“飞机突然加速”的现象完全吻合。
- 注：[spawnEnemy()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L126-L138) 用 `Date.now()` 节流，通常不会双倍生成敌机，但碰撞与扣血判定也会跑两遍，节奏会异常。

### 为什么会“卡顿”

- **绘制翻倍且相互覆盖**：两条链在同一帧的回调批次里都会执行 `drawBackground()`（整屏清空）再重绘。第二条链会覆盖第一条链的画面，视觉上只看到最后一帧，但状态已经前进两步，徒增一倍 CPU/GPU 开销。
- **调度错位导致帧 pacing 不均**：旧链 A' 是在 t0 调度的，新链 B' 是在 t2 调度的，两者相差若干毫秒。于是有的动画帧里两条回调落在同一 tick（表现为一次跳两格），有的落在相邻 tick（表现为连续两小步），节奏忽快忽慢 → 视觉抖动/卡顿。
- **工作量翻倍挤占帧预算**：60fps 下每帧预算约 16ms，双倍更新+绘制可能导致超时掉帧，掉帧又会拉长“暂停→继续”的竞态窗口，形成负反馈，卡顿更明显。
- 链数是否会继续累积？**不会无限增长**：再次点击“暂停”时，两条链的在途帧都会读到 `isPaused=true` 而双双自然终止；点“继续”只重启一条链。因此单次事故最多产生 2 条链，但 2 条已足以造成明显加速与卡顿。

### 为什么“偶尔”出现

- 只有当“暂停点击”与“继续点击”落在同一个动画帧间隔内（60Hz 约 ≤16ms，掉帧时更宽）才会触发。
- 正常速度的单击暂停、停顿一下再继续，间隔远大于一帧，旧在途帧早已自然死亡，因此不会复现。
- 快速双击、按键抖动、浏览器繁忙掉帧、标签页切回等时机，更容易把两次操作挤进同一帧窗口，所以表现为“偶尔”。

---

## 四、其他次要风险路径

### 重新开始 + 立即开始（快速连点）

- [restartGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L88-L114) 置 `isPlaying=false`，但同样**不取消在途帧**。
- 若在旧链的在途帧触发之前就点击“开始”（[startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L70-L76) 置 `isPlaying=true` 并直接调用 `gameLoop()`），则：
  - 新链 B 启动；
  - 旧在途帧随后触发，读到 `isPlaying=true` → 不返回 → 旧链 A 复活。
- 结果与第三节相同：双重循环、双倍更新。
- 触发概率低于暂停/继续，因为 UI 上“重新开始”在结算层、“开始”在控制区，需要两次独立点击；但通过快速连点/键盘回车/自动化仍可能在一帧内完成。

### 游戏进行中重复点击“开始”

- [startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L73) 会在开始后 `startBtn.disabled = true`，进行中无法再次点击“开始”，因此该入口被 UI 屏蔽，**不会触发双重循环**。

---

## 五、为什么“重启后正常”

- [restartGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L88-L114) 把 `isPlaying` 置为 `false`。
- 无论当时有几条链在跑（1 条或 2 条），它们各自的在途帧下一次触发时都会在 [第426行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L426) 因 `!isPlaying` 而 `return`，且不再调度下一帧 → **所有循环链全部死亡**。
- 同时清空了 `bullets`、`enemies`，重置了玩家位置、分数、生命。
- 之后再点“开始”，只会建立一条干净的单链 → 速度与节奏恢复正常。
- 这与用户描述的“重启后正常”完全一致，反向印证了根因是“多余的循环链未被取消”。

---

## 六、结论汇总

| 路径 | 是否调用 `gameLoop()` | 是否取消旧帧 | 是否会产生双重循环 |
| --- | --- | --- | --- |
| 开始游戏 [startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L70-L76) | 是 | 否（无需） | 否（开始按钮在游戏中被禁用） |
| 暂停 [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L78-L86) | 否 | **否** | 否（但留下未取消的在途帧，埋下隐患） |
| 继续 [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L78-L86) | 是 | **否** | **是 — 当与暂停间隔小于一帧时，旧在途帧复活，双链并存** |
| 游戏结束 [checkGameOver()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L213-L221) | 否 | 否 | 否 |
| 重新开始 [restartGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-athena/frontend/script.js#L88-L114) | 否 | 否（靠 `isPlaying=false` 让旧链自然死亡） | 本身否；与“开始”极快连点时存在次要风险 |

**根因一句话**：暂停时仅用 `isPaused` 标志让循环“自然停止”，而没有 `cancelAnimationFrame` 取消已调度的在途帧；当“继续”在该在途帧触发前到达，`isPaused` 被改回 `false` 并启动新链，旧在途帧随后触发时因状态已恢复而“复活”，形成两条并行 rAF 链，导致每帧双倍更新（加速）与调度错位/双倍开销（卡顿）。重新开始通过置 `isPlaying=false` 杀死所有链，所以重启后恢复正常。

> 本报告仅做问题分析，未对任何代码进行修改。
