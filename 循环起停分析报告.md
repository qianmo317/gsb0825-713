# 打飞机游戏「偶发加速 / 卡顿」问题分析报告

> 分析对象：`frontend/script.js`
> 结论：**存在“双循环并发”隐患**，会导致每帧被更新两次，表现为“飞机突然加速、画面卡顿”，且可随触发次数累积（3 倍、4 倍…）。本报告仅做分析，未修改任何代码。

---

## 一、动画循环是怎么驱动的

核心循环函数 `gameLoop()`（`script.js:425-445`）：

```js
function gameLoop() {
    if (!gameState.isPlaying || gameState.isPaused) {   // ← 唯一的“停止”判据
        return;
    }
    // 更新：updatePlayer / updateBullets / updateEnemies / spawnEnemy / checkCollisions
    // 绘制：drawBackground / drawPlayer / drawBullets / drawEnemies
    requestAnimationFrame(gameLoop);                     // ← 自我递归调度
}
```

两个关键事实：

1. **没有保存 `requestAnimationFrame` 的返回句柄，全程也没有任何 `cancelAnimationFrame` 调用。**
   一条循环链一旦被 `requestAnimationFrame` 排上队，就无法被主动取消。
2. **循环的“停止”完全依赖下一帧执行时那句 `if (!isPlaying || isPaused) return` 的守卫判断。**
   也就是说：停止是**异步的、被动的**——只有等到下一帧真正回调进来、发现标志位不满足，链条才会自然终止。

把“循环链条”想成一条自我延续的链：只要每一帧守卫都通过，它就一直存活并不断预约下一帧；某一帧守卫失败，这条链就死亡。

- **新链的诞生点**（会调用 `gameLoop()` 从而启动一条链）：
  - `startGame()`（`script.js:75`）
  - `togglePause()` 里“继续”分支（`script.js:82-84`）
- **链的死亡点**：某一帧进入 `gameLoop()` 时 `isPlaying === false` 或 `isPaused === true`。

---

## 二、五条路径下循环如何起停（时间线）

### 1) 开始（startGame，`script.js:70-76`）
- 置 `isPlaying=true, isPaused=false`，禁用开始键、启用暂停键，调用 `gameLoop()`。
- 稳态：**恰好 1 条链**在跑，任意时刻只有 1 个待触发的 rAF 回调。

### 2) 暂停（togglePause 关闭方向，`script.js:78-86`）
- 置 `isPaused=true`。**不取消**当前已排队的那个 rAF。
- 下一帧：待触发回调进来 → 守卫命中 `isPaused` → `return` → **链死亡**，pending 归零。
- 正常人手速下（暂停后停留 >16ms），链会先死掉。

### 3) 继续（togglePause 打开方向，`script.js:82-84`）
- 置 `isPaused=false`，**立即同步调用 `gameLoop()`** → 诞生一条新链 B，并预约一个 rAF。
- 若“继续”发生在“暂停”后旧链已死之后 → 只剩链 B，正常。

### 4) 结束（checkGameOver，`script.js:213-221`）
- `lives<=0` 时置 `isPlaying=false`，禁用开始/暂停键，显示结束层。
- 注意：触发结束的这一帧 `gameLoop()` **仍会继续执行到 `script.js:444`，无条件再预约一个 rAF**（守卫只在函数开头判断一次）。
- 因此结束后仍残留**一个待触发的 rAF**；它在下一帧进来时守卫失败而死亡。

### 5) 重新开始（restartGame，`script.js:88-114`）
- 置 `isPlaying=false, isPaused=false`，重置分数/生命/对象/玩家位置，重新启用开始键、禁用暂停键，清空画布。
- 同样**不取消**任何已排队的 rAF；靠“下一帧守卫失败”让旧链自然死亡。

---

## 三、会不会出现“两条循环同时在跑、一帧更新两次”？——会

根本原因（一句话）：

> **标志位（isPlaying / isPaused）是同步瞬间翻转的，而旧链的 rAF 回调是异步、延后一帧才检查守卫的；只要在“旧链那次待触发回调尚未 fire”的窗口内又新建了一条链，旧链就来不及死亡，两条链就会并存。** 由于守卫只看当前标志、不认“链的身份”，一旦标志被重新置回可运行状态，旧的待触发回调会“复活”继续跑。

两条链并存后，每个真实动画帧里 `updatePlayer/updateBullets/updateEnemies` 各执行两次，位移是**按帧数累加固定像素**（如 `bullets[i].y -= speed`、`player.x += speed`，见 `script.js:142-152`、`157`、`168`），并非按时间差 delta 计算，所以**两次更新 = 双倍位移 = 突然加速**；同时绘制也翻倍、rAF 回调数量翻倍 → **卡顿**。若再次触发还会叠成 3 倍、4 倍，越来越糟。

### 触发路径 A：暂停后“极快地”继续（连点/狂点暂停键）
- 时间线：
  1. 稳态 1 条链 A，已排队 1 个 pending 回调。
  2. 点“暂停”：`isPaused=true`，A 的 pending 回调**未取消，仍在队列里**。
  3. 在这个 pending 回调 fire 之前（即同一帧、约 16ms 窗口内）再点“继续”：`isPaused=false` 并同步调用 `gameLoop()` → 诞生链 B 并排队 B 的回调。此刻队列里有 **A、B 两个 pending**。
  4. 下一帧：A、B 两个回调都进来，守卫都满足（`isPlaying && !isPaused`）→ **两条链都存活并各自续帧** → 双循环。
- 触发条件：**“暂停 → 继续”两次点击落在同一个动画帧窗口内（≈16ms）**。

### 触发路径 B：结束/重开后“极快地”重新开始（重开→开始 连点）
- 时间线：
  1. 游戏中链 A 运行（或刚 game over，残留 1 个 pending，见上文第 4 点）。
  2. 点“重新开始”：`isPlaying=false`，A 的 pending **未取消**；`startBtn` 被重新启用。
  3. 在 A 的 pending fire 之前又点“开始”：`startGame()` 置 `isPlaying=true` 并同步调用 `gameLoop()` → 诞生链 B。
  4. 下一帧：A 的旧回调进来时 `isPlaying` 又变回 `true`、`isPaused=false`，守卫通过 → **A 复活**；B 也在跑 → 双循环。
- 触发条件：**“重新开始 → 开始”两次点击落在同一个动画帧窗口内（≈16ms）**。

### 为什么是“偶尔”出现、重启后正常
- 两条路径都需要“两次点击落进同一帧（约 16ms）”的窄窗口，正常手速一般跨帧，旧链早已死亡，所以**大多数时候正常**。
- 但两点会显著放大命中概率，形成“偶发”：
  1. **快速双击 / 焦急连点**暂停键或重开+开始，容易把两次操作压进 16ms 内。
  2. **掉帧/长帧**（GC、后台负载、标签页切换导致 rAF 被节流冻结等）会让“下一帧回调”迟迟不 fire，窗口从 16ms 拉长到几十上百毫秒，**普通点击也可能落入窗口** → 更易复现。
- **刷新页面 / 重开浏览器**会清空所有排队的 rAF 链，回到“单链”稳态，所以“重启后正常”。这也解释了用户观察到的现象。

### 补充：一旦进入双循环，还会自我恶化
- 双链状态下再触发一次路径 A/B，可叠成 3 链、4 链……速度和卡顿逐级加重，直到刷新页面为止。
- `startGame()`/`togglePause()` 都**没有“已有循环在跑就不再新开”的自检**，`gameLoop()` 也没有“同一时刻只允许一条链”的去重/句柄管理，是问题得以成立的直接代码层面缺口。

---

## 四、结论小结

| 项目 | 说明 |
|---|---|
| 现象 | 飞机突然加速 + 画面卡顿，偶发，刷新后恢复 |
| 直接机制 | 两条（或更多）`gameLoop` 链并发，每帧更新/绘制被执行多次；位移按帧累加故直接翻倍 → 加速 |
| 根本原因 | 未保存 rAF 句柄、从不 `cancelAnimationFrame`；停止依赖“下一帧守卫”，而标志位是同步翻转、旧回调异步延后检查，存在竞态窗口 |
| 触发路径 A | 暂停→继续两次点击落入同一帧窗口（≈16ms），旧链未死 + 新链诞生 |
| 触发路径 B | 重新开始→开始两次点击落入同一帧窗口，旧链因 `isPlaying` 被重新置真而“复活” |
| 放大因素 | 快速连点、掉帧/长帧、标签页切换致 rAF 冻结，均加宽竞态窗口 |
| 为何偶发/重启正常 | 命中需落入窄窗口故偶发；刷新清空所有 rAF 链，回到单链稳态 |

> 说明：本报告按要求仅做分析与定位，未对任何源码进行改动。修复方向（供参考，未实施）：用一个变量保存 `requestAnimationFrame` 返回的句柄，在 `startGame`/`togglePause 继续`/`restartGame`/暂停/结束等入口处先 `cancelAnimationFrame` 掉旧句柄，或在 `gameLoop` 里加“单链守卫”保证任意时刻只有一条链在跑。
