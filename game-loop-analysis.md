# 打飞机游戏：requestAnimationFrame 循环起停时间线分析报告

> 分析对象：`frontend/script.js`（唯一的游戏逻辑文件）
> 结论先行：**确实存在"两条动画帧循环同时运行"的可能**，根源是整个循环没有任何取消/去重机制（无 `cancelAnimationFrame`、无句柄、无代际令牌），起停完全依赖 `isPlaying` / `isPaused` 两个布尔标志在回调执行瞬间的值。本文只做分析，不修改代码。

---

## 1. 循环的驱动机制

- 唯一的循环体是 `gameLoop()`（script.js:425-445），结构为：

  1. 开头守卫：`if (!gameState.isPlaying || gameState.isPaused) return;`（script.js:426-428）
  2. 同步执行一次 update + draw
  3. 末尾 `requestAnimationFrame(gameLoop)`（script.js:444）把自己挂到下一帧

- 关键特征：
  - **循环靠递归 rAF 自我续期**，每一帧都无条件再预约下一帧；停链的唯一方式是某一帧回调触发时守卫不通过、`return`，不再预约。
  - **整条链路没有保存 rAF 句柄**，`cancelAnimationFrame` 在代码中出现 0 次。任何时刻都无法主动取消一个已经预约、但尚未执行的帧回调。
  - **三个入口（开始 / 继续）都是"同步直接调用 `gameLoop()`"**，而不是"只设置标志、交给下一帧启动"。这意味着每起一条新链，就必然产生一个新的 pending rAF。
  - update 全部使用**固定步长**（`player.speed`、`bullet.speed`、`enemy.speed`），与帧时间戳无关，没有 deltaTime 补偿。因此"每帧多跑几次 update"直接等于"速度快几倍"。

## 2. 五条路径下循环的起停时间线

### 2.1 开始（`startGame`，script.js:70-76）

- 点击"开始游戏" → 置 `isPlaying=true, isPaused=false`，禁用开始按钮 → **同步调用一次 `gameLoop()`**。
- 这一次同步调用本身就完成了一帧 update+draw，并在末尾预约 rAF → 链 A 建立，之后逐帧自续。
- 因 `startBtn.disabled = true`，无法通过连点开始按钮起第二条链（此入口有按钮态保护）。

### 2.2 暂停（`togglePause` 的前半段，script.js:78-86）

- 点击"暂停游戏" → `isPaused=true`。**此时不做任何取消动作**。
- 链 A 上还有一个之前预约好、尚未执行的 rAF。它会在下一帧照常触发，回调里读到 `isPaused=true` → 守卫 `return` → 不再预约 → **链 A 在"暂停后的一帧之内"自然死亡**。
- 即：暂停不是即时停链，而是"延迟最多一帧、靠守卫自然断链"。在这一个帧间隙内，旧链的 pending rAF 仍然活着。

### 2.3 继续（`togglePause` 的后半段，script.js:82-84）

- 点击"继续游戏" → `isPaused=false` → **同步调用一次 `gameLoop()`** → 立即跑一帧并预约 rAF → 链 B 建立。
- 设计意图：此刻链 A 已死（见 2.2），链 B 是唯一在跑的循环。**但这个前提只在"暂停后至少过了完整一帧才点继续"时成立**（详见第 3 节）。

### 2.4 结束（`checkGameOver`，script.js:213-221）

- 生命归零 → `isPlaying=false`，显示结束面板，禁用开始/暂停按钮。
- 与暂停同理：**不做取消**，链 A 的 pending rAF 在下一帧触发时读到 `isPlaying=false` → `return` → 链 A 自然死亡。同样是"延迟最多一帧"的软停止。

### 2.5 重新开始（`restartGame`，script.js:88-114）

- 点击"重新开始" → 重置全部状态（`isPlaying=false, isPaused=false`）、清空对象、启用开始按钮。
- **此函数本身不起链、也不取消链**。如果点击时游戏正在进行，旧链 A 的 pending rAF 仍活着，同样靠"下一帧读到 `isPlaying=false` 后 return"软停止。
- 真正的起链发生在之后点击"开始游戏"（回到 2.1）。

### 路径小结

| 路径 | 起链方式 | 停链方式 | 停链是否即时 |
|---|---|---|---|
| 开始 | 同步调 `gameLoop()` + rAF 自续 | — | — |
| 暂停 | — | 守卫 return，不再预约 | 否，延迟 ≤1 帧 |
| 继续 | 同步调 `gameLoop()` + rAF 自续 | — | — |
| 结束 | — | 守卫 return，不再预约 | 否，延迟 ≤1 帧 |
| 重新开始 | 不起链（等"开始"） | 与"结束"相同的软停止 | 否，延迟 ≤1 帧 |

**"停链延迟一帧"与"起链即时同步"这两个特性的组合，就是双链问题的全部来源。**

## 3. 双循环并发的触发路径与条件

### 3.1 双链成立的充分条件

同时满足三件事：

1. 旧链 A 上还有一个**已预约、尚未执行**的 rAF（即处于"停链延迟一帧"的窗口内）；
2. 在该回调执行前，`isPlaying`/`isPaused` 被**翻回运行态**，使旧回调的守卫不再拦截；
3. 同一时机某入口又**同步调用了一次 `gameLoop()`**，建立链 B。

此时下一帧：旧回调照常执行（守卫已通过）并预约下一帧，链 A 复活；链 B 也在自续。**两条链各自每帧续约、互不感知，从此永久并行**（直到下一次暂停/结束把两条一起杀死，因为守卫判的是同一个共享标志）。

### 3.2 路径 A：暂停 → 继续（极快连点，同一帧间隙内）

- 时序：
  1. 第 N 帧渲染后，链 A 已预约第 N+1 帧的回调；
  2. 用户点"暂停" → `isPaused=true`；
  3. **在第 N+1 帧回调执行之前**（窗口约 0~16ms），用户又点"继续" → `isPaused=false` 且同步调 `gameLoop()` → 链 B 建立并预约 rAF；
  4. 第 N+1 帧：链 A 的旧回调触发，读到的是**已经翻回的** `isPaused=false` → 守卫通过 → update+draw+续约 → 链 A 复活；
  5. 从此链 A、链 B 并行。
- 触发条件：暂停与继续两次点击落在同一个帧间隙内。手动点击很难做到，但双击、触屏快速连点、按键/自动化脚本、低帧率设备（帧间隙被拉长，窗口变大）都可能命中——与"偶尔出现"的现象吻合。

### 3.3 路径 B：游戏刚结束 → 重新开始 → 开始（旧链最后一帧未执行前）

- 时序：
  1. `checkGameOver` 置 `isPlaying=false`，但链 A 的 pending rAF 还活着；
  2. 在下一帧触发前，用户点"重新开始"（`restartGame` 重置状态、启用开始按钮）再点"开始游戏" → `startGame` 置 `isPlaying=true` 并同步调 `gameLoop()` → 链 B 建立；
  3. 旧回调随后触发，读到 `isPlaying=true` → 守卫通过 → 链 A 复活 → 双链并行。
- 触发条件：从结束画面出现到点完"重新开始→开始游戏"全程压缩在一个帧间隙内。窗口极小，但同样因无取消机制而毫无防护。

### 3.4 路径 C：游戏进行中点重新开始 → 立刻点开始

- 时序：
  1. 链 A 运行中，用户点"重新开始" → `isPlaying=false`（软停止，pending rAF 仍活）；
  2. 同一帧间隙内点"开始游戏" → `isPlaying=true` + 同步 `gameLoop()` → 链 B；
  3. 旧回调触发时守卫已通过 → 链 A 复活 → 双链。
- 触发条件：同 3.3，本质是"软停止窗口内又把标志翻回运行态"。

### 3.5 为什么常规操作不会触发

- 暂停后隔了任意超过一帧的时间再点继续：旧回调早已触发并 `return`，链 A 死透，继续只起一条链——**这是代码隐含依赖的前提**。
- 结束/重开后隔一帧以上再点开始：同理安全。
- 即：**只要"软停止的一帧延迟"被操作速度击穿，就必然双链**。代码对此没有任何防御。

## 4. 双链后的表现与"飞机突然加速、画面卡顿"的对应关系

- **速度翻倍（"飞机突然加速"）**：两条链在同一帧各执行一次完整 update。玩家飞机每帧位移 `2×playerSpeed`、子弹 `2×bulletSpeed`、敌机 `2×enemySpeed`，全部固定步长逻辑双倍推进；玩家-敌机碰撞、敌机越界扣血也双倍判定，生命消耗更快。因无 deltaTime，这就是纯粹的 2 倍速。
- **画面卡顿**：每帧 update×2 + draw×2，Canvas 绘制调用量翻倍，主线程单帧耗时上升，在性能余量不大的设备上直接掉帧、帧间隔抖动，表现为卡顿；且"加速"与"掉帧"并存时体感就是忽快忽卡。
- **为何重启后正常**：暂停/结束/重开的守卫判的是共享标志，下一次暂停或结束会让两条链在同一帧一起 `return` 断掉；刷新页面或重开一局（先让双链同时死亡）即恢复单链。这解释了"重启后正常"。
- **为何偶尔**：触发依赖"操作落在软停止的一帧窗口内"这一时序竞争，正常使用极少命中，属概率性 bug。

## 5. 根因小结

1. **无取消机制**：全代码无 `cancelAnimationFrame`，不保存 rAF 句柄，已预约的回调无法撤销，停止只能靠"下一帧回调里读标志后 return"——停链天然有一帧延迟。
2. **起链与停链不对称**：起链是入口同步直调 `gameLoop()`（即时），停链是守卫软停止（延迟一帧）。两条路径在时间轴上存在交叠窗口。
3. **守卫读的是易变的共享状态**：旧回调是否断链取决于它执行那一刻 `isPlaying/isPaused` 的值，而不是"它属于哪一局/哪条链"。标志在窗口内被翻回，旧链就复活。
4. **无代际/单例保护**：没有任何"当前只允许一条链"的运行令牌（如递增的 loopId 比对），因此第二条链建立时不会使第一条链失效。
5. **固定步长更新**：update 与帧时间戳解耦，链的条数直接倍乘游戏速度，使双链问题在视觉上立刻放大为"加速"。

（按任务要求，本报告仅做分析，未对代码做任何修改。）
