# 打飞机游戏 —— 动画帧循环起停路径分析报告

> 分析对象：`frontend/script.js`
> 问题现象：游戏中偶尔出现"飞机突然加速、画面卡顿"，重启（重新开始 / 刷新页面）后恢复正常。
> 结论速览：**问题属实。存在"多条 `requestAnimationFrame` 循环并行、同一帧内游戏状态被更新两次"的路径。触发点是「暂停 → 继续」两次点击落在同一个浏览器动画帧间隔（约 16.7ms）内；一旦触发，多余循环会自我延续、不会自愈，直到游戏结束或页面刷新。**

---

## 一、循环机制的基本事实

游戏只有一个循环函数 [gameLoop()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L425-L445)，它是"递归驱动"的：

- 函数尾部通过 `requestAnimationFrame(gameLoop)`（[script.js 第 444 行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L444-L444)）预约下一帧，形成自延续链。
- **停止方式是"软停止"**：函数开头的守卫语句 `if (!gameState.isPlaying || gameState.isPaused) { return; }`（[script.js 第 426-428 行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L426-L428)）——一旦发现不该跑，就 `return` 且**不再预约下一帧**，链条靠"下一帧自杀"终止。
- 全代码中**没有 `cancelAnimationFrame`，也没有保存 rAF 返回的句柄 id**。也就是说：任何代码都无法主动撤销"已经排队等待下一帧执行的那个回调"。
- 谁会启动循环（调用 `gameLoop()`）：只有两处——
  1. [startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L70-L76)（开始游戏按钮）；
  2. [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L78-L86) 中从暂停切回继续时（第 83 行）。

关键推论：**`gameLoop()` 每被调用一次，就会诞生一条新的 rAF 自延续链；系统从不检查"是否已有一条链活着"。** 旧链能否死掉，完全取决于它排队中的那个回调"有没有机会在状态被改回去之前，执行一次并看到停止标志"。

---

## 二、五条路径的时间线梳理

### 路径 0：页面加载（初始态）

- [initGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L448-L453) 只静态绘制背景和飞机，**不启动循环**。
- 状态：`isPlaying=false, isPaused=false`；无 rAF 排队。画面静止。

### 路径 1：开始游戏

- 点击"开始游戏" → [startGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L70-L76)：置 `isPlaying=true, isPaused=false`，禁用开始按钮、启用暂停按钮，然后调用 `gameLoop()`。
- `gameLoop()` **同步**执行第 1 帧（更新 + 绘制），尾部 `requestAnimationFrame(gameLoop)` 预约第 2 帧 → 链 A 诞生，此后每帧自延续。
- 防重入保护：开始按钮在游戏中被 `disabled`（第 73 行），游戏结束时也保持禁用（[checkGameOver() 第 218 行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L213-L221)），只有重新开始后才恢复（[restartGame() 第 108 行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L88-L114)）。**此路径安全，不会重复起链。**

### 路径 2：暂停

- 点击"暂停游戏" → [togglePause()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L78-L86)：`isPaused=true`，按钮文案变"继续游戏"；因为此时 `isPaused` 为 true，**不调用** `gameLoop()`。
- 注意：此刻链 A **并没有被取消**——上一帧尾部预约的回调仍在浏览器 rAF 队列里。
- 到**下一个动画帧**，该回调触发，守卫命中 `isPaused=true` → 直接 `return`，不再预约 → 链 A 死亡。
- 特点：停止有 **1 帧的滞后**，但死亡前不做任何更新/绘制，所以暂停画面正确。**前提是：这个"待触发回调"真的有机会在暂停状态下执行一次。**

### 路径 3：继续游戏（⚠️ 事故路径）

- 再次点击按钮（文案已是"继续游戏"）→ `togglePause()`：`isPaused=false`，由于 `!isPaused` 成立，**无条件**调用 `gameLoop()`（第 82-84 行）。
- 这次调用同步跑一帧，并预约新链 B 的回调。
- 是否安全，取决于一个时序条件：
  - **暂停持续时间 ≥ 1 个动画帧（约 16.7ms，正常人为操作都满足）**：链 A 排队中的回调早已在暂停期间触发过、看到 `isPaused=true` 并自杀。此时起链 B，全游戏只有 1 条链 → **正常**。
  - **暂停 → 继续发生在同一个动画帧间隔之内（两次点击间隔 < 约 16.7ms）**：链 A 的待触发回调**还没来得及执行**，从未看到过 `isPaused=true`；而继续动作已经把 `isPaused` 改回 false 并起了链 B。下一个动画帧到来时，**A、B 两个回调同时触发**，两者都看到 `isPlaying=true && isPaused=false`，守卫双双放行 → **两条链并行，且各自都会继续预约下一帧，永久并存**。详见第三节。

### 路径 4：游戏结束

- 生命耗尽发生在某一帧的更新中途：[updateEnemies()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L166-L178) 或 [checkCollisions()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L180-L204) 调 [checkGameOver()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L213-L221)，置 `isPlaying=false`、显示结束遮罩、禁用两个按钮。
- 两个细节：
  1. `isPlaying=false` 是在帧**中间**被设置的，但当前帧不会中断——后续绘制照常执行，且帧尾的 `requestAnimationFrame(gameLoop)` **仍会预约一次**（第 444 行无条件执行）。
  2. 这个"多余预约"的回调在下一帧触发时守卫命中 `isPlaying=false` → 自杀，不再延续。**多一条排队回调但无害。**
- 若当时已有多条链并行（见路径 3 的事故态），同一帧内先执行的链把 `isPlaying` 置 false 后，后执行的链在帧顶部守卫即被拦下；下一帧所有链全部死亡。**游戏结束是天然的"清场"点。**

### 路径 5：重新开始

- 点击"重新开始"（该按钮位于结束遮罩内，遮罩默认 `display:none`，仅游戏结束后可见、可点）→ [restartGame()](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L88-L114)：
  - 置 `isPlaying=false, isPaused=false`，清零分数/生命、清空子弹和敌机数组、复位玩家、隐藏遮罩、恢复开始按钮、禁用暂停按钮、清空画布；
  - **不启动循环**，回到与路径 0 相同的待开始状态。
- 此后用户再点"开始游戏" → 走路径 1，起**一条**全新链。由于游戏结束时旧链已全部死亡（路径 4），此处不会叠加。**这就是"重新开始后恢复正常"的原因。**

---

## 三、核心问题：双循环（多循环）并行是如何发生的

### 3.1 触发路径与精确时序

**触发路径：路径 3「暂停 → 继续」，条件是两次点击落在同一个动画帧间隔内（< 约 16.7ms）。**

典型场景：在"暂停游戏"按钮上快速双击（鼠标按键抖动/连击、触摸板轻触两下、玩家紧张时连点），第一下暂停、第二下继续，两下间隔短于一帧。

逐帧时序（假设链 A 正在运行）：

| 时刻 | 事件 | rAF 队列 / 状态 |
|---|---|---|
| 帧 N | 链 A 回调执行更新+绘制，帧尾预约回调 **A1** | 队列：[A1]；`isPlaying=true, isPaused=false` |
| 帧 N ~ N+1 之间（<16.7ms 内） | 第 1 次点击：`isPaused=true` | 队列：[A1]（A1 **未被取消，也未执行**） |
| 同一间隔内 | 第 2 次点击：`isPaused=false` → 调 `gameLoop()`：同步更新+绘制 1 次，并预约 **B1** | 队列：[A1, B1]；`isPlaying=true, isPaused=false` |
| **帧 N+1** | A1 触发：守卫放行 → 更新+绘制 → 预约 A2；**紧接着** B1 触发：守卫也放行 → **再次更新+绘制** → 预约 B2 | 队列：[A2, B2] |
| 帧 N+2 及以后 | A2、B2 每帧双双触发，双双自预约…… | **两条链永久并行** |

要点：

1. **A1 从头到尾没有一帧看到过 `isPaused=true`**。软停止机制要求"旧链在下一帧自杀"，但暂停态存活时间不足一帧，旧链连自杀的机会都没有。
2. 继续时 `gameLoop()` 是**无条件启动**的（第 82-84 行只判断 `!isPaused`，不判断是否已有链在跑），于是新链 B 直接叠加。
3. 浏览器对同一帧内排队的多个 rAF 回调会**按入队顺序全部执行**，不会去重——它们虽然指向同一个函数，却是两个独立的回调条目。
4. 事故可以累积：在同一帧间隔内完成更多轮"暂停→继续"（如 4 连击），每轮继续都会新增一条链，可出现 3 条、4 条链并行。

### 3.2 为什么表现为"飞机突然加速"

每条链每帧都会完整执行一遍更新逻辑（[gameLoop 第 431-435 行](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0825/source%20code/gsb0825-713/gsb0825-713-odysseus/frontend/script.js#L430-L435)）：

- `updatePlayer()`：玩家飞机每帧移动 `speed=5` 像素；两条链 → 每帧移动 10 像素，**手感速度 ×2**。
- `updateBullets()`：子弹速度 8 → 等效 16/帧；`updateEnemies()`：敌机速度 2 → 等效 4/帧。
- 所有运动都是"每帧固定步长"、与帧间真实时长无关（没有用时间差 deltaTime 归一化），所以更新次数翻倍 = 游戏速度翻倍。
- 视觉上就是：**点了几下暂停/继续之后，飞机、子弹、敌机突然整体变快**。

### 3.3 为什么伴随"画面卡顿"

- 每帧要执行 **2 遍更新 + 2 遍全画布绘制**（`drawBackground()` 每次都整块重刷 800×600 画布），单帧 CPU 工作量近似翻倍；在性能一般的机器上容易超过 16.7ms 帧预算 → 掉帧、卡顿。
- 两条链在同一帧内先后更新同一批数组（`bullets`、`enemies`），虽不报错（第二遍只是在新状态上再推进一次），但双倍的 splice/遍历进一步加重主线程负担。
- 绘制两遍时第二遍覆盖第一遍，所以**不会**出现残影/拖影，症状集中在"变快 + 掉帧"。

### 3.4 为什么"偶尔发生"且"重启后正常"

- **偶发性**：触发窗口只有一个动画帧（约 16.7ms），需要两次点击恰好落在同一窗口内，正常点击（暂停后看一眼再继续，间隔动辄几百毫秒以上）不会触发；只有快速双击/连点才命中，所以是小概率事件。
- **不自愈**：一旦两条链都起来，每帧各自 `requestAnimationFrame` 自我延续，没有任何机制能发现并撤掉多余的链，事故态会一直持续到本局结束。
- **能被"正常暂停"意外修复**：如果事故后再来一次**慢速**暂停（持续超过 1 帧），所有链排队中的回调都会在暂停期间触发、命中 `isPaused=true` 而集体死亡；再继续时只起一条链 → 又恢复正常。这解释了"有时候再暂停一下又好了"。
- **重启必然恢复**：游戏结束置 `isPlaying=false`（路径 4），下一帧所有链集体自杀；重新开始后再点开始只起一条链（路径 5）。刷新页面更彻底，rAF 队列随页面销毁。这与"重启后正常"完全吻合。

---

## 四、根因归纳

1. **停止靠"软停止"，且有一帧滞后**：`gameLoop()` 开头的守卫只能让"下一帧的回调"停止自延续，无法撤销已排队的回调；暂停/结束状态若存活不足一个动画帧，旧链就看不到停止信号。
2. **启动无防重入**：继续游戏时 `gameLoop()` 被无条件调用，系统不记录 rAF 句柄、不检查是否已有循环在跑；全项目没有 `cancelAnimationFrame`。
3. **"软停止 + 无条件重启"组合在暂停时长 < 1 帧时失效**：旧链未死、新链已生，造成 N 条 rAF 链并行；每帧更新 N 次 → 速度 ×N、单帧工作量 ×N → 加速 + 卡顿。
4. 加重体感的设计因素：运动步长按"帧"固定、未按真实时间差归一化，更新次数翻倍直接等价于游戏速度翻倍。

## 五、其他路径的结构性风险（理论存在，UI 上人工不可达）

- **结束 → 重新开始 → 开始 的一帧竞态**：`restartGame()` 置 `isPlaying=false` 同样是软停止，若旧链排队回调尚未执行，而用户在同一帧间隔内完成"重新开始 + 开始游戏"两次点击使 `isPlaying` 迅速变回 true，旧回调会被"复活"并与新链并行。但结束遮罩要等帧绘制后才可见可点，且需要在两个不同按钮上 16.7ms 内连点两次，人工操作不可达，现实中不会发生。
- **游戏结束当帧仍预约一次 rAF**（第 444 行无条件执行）：多余一次排队，下一帧被守卫拦截，无害。
- **继续瞬间的同步更新**：`togglePause()` 里直接调用 `gameLoop()` 会在点击事件中同步执行一次更新+绘制，使恢复瞬间多出一步位移，影响轻微。

---

*本报告仅作分析，未对任何代码进行修改。*
